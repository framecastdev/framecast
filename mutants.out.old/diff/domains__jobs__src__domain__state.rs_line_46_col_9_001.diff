--- domains/jobs/src/domain/state.rs
+++ replace JobState::is_terminal -> bool with false
@@ -38,17 +38,17 @@
     Completed,
     Failed,
     Canceled,
 }

 impl JobState {
     /// Check if this is a terminal state
     pub fn is_terminal(&self) -> bool {
-        matches!(self, Self::Completed | Self::Failed | Self::Canceled)
+        false /* ~ changed by cargo-mutants ~ */
     }

     /// Get all valid next states from current state
     pub fn valid_transitions(&self) -> &'static [JobState] {
         match self {
             Self::Queued => &[Self::Processing, Self::Canceled],
             Self::Processing => &[Self::Completed, Self::Failed, Self::Canceled],
             Self::Completed => &[],
